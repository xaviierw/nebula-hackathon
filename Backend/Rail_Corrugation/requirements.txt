# Rail Corrugation subsystem dependencies.
#
# Split deliberately: the API installs only the INFERENCE tier (see
# Backend/requirements.txt). Training dependencies must never become an API
# dependency.

# --- INFERENCE (always required) ---
pandas>=2.2
numpy>=2.2

# --- TRAINING (only needed to refit the model) ---
# scikit-learn>=1.5
# joblib>=1.4
