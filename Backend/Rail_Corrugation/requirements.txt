# Exact inference environment encoded in model/rail_corrugation.joblib.
# The saved scikit-learn estimator is version-sensitive, so deployments must
# use these versions rather than merely installing a recent release.

# --- INFERENCE (shared API + CLI predict) -------------------------------
numpy==2.3.3
pandas==2.3.3
scipy==1.16.2
scikit-learn==1.7.2
joblib==1.5.3

# --- TRAINING / EVALUATION ONLY -----------------------------------------
# matplotlib>=3.9
# seaborn>=0.13
# jupyter>=1.1
